# Nothing generated lives in this repo. Runtime state is deliberately kept
# outside it: ~/.config/omarchy/whatsmarchy/ (settings) and
# ~/.cache/omarchy-whatsmarchy/ (downloaded media).
*.swp
*~
.DS_Store
